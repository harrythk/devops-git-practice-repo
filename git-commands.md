# Setup & Config Commands

git init

git clone <repo-url>

git remote -v


# Basic Workflow

git add <file_name>

git commit -m "<message>"

git push origin <branch>

git pull origin <branch>

git branch


# Viewing Changes

git log --oneline

git status
